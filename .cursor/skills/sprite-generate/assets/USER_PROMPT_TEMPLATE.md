Canvas:
  Aspect ratio: [W:H, e.g. 4:5]   (≈ [decimal, e.g. 0.80:1])
  Orientation: PORTRAIT — taller than wide. The canvas height must be
  visibly greater than the canvas width; never produce a square or
  landscape composition.
  Pixel-art look: the image must read as an upscale of a small pixel grid —
  uniform square blocks, hard edges, no sub-pixel detail, no anti-aliasing,
  no detail thinner than one logical pixel.
  Transparent background (PNG alpha). Center the sprite; no padding or
  borders beyond what the pose requires.

Subject:
  Humanoid figure standing upright. Head on top, torso center, two arms,
  two legs. The figure should occupy roughly the full canvas height
  (head near the top edge, feet near the bottom edge) so the portrait
  aspect ratio is visually obvious.

Palette constraint: [PASTE PALETTE SUMMARY TABLE FROM CHARACTER BRIEF]

[PASTE FULL COMPLETED CHARACTER BRIEF HERE]

This frame:
  FILE:  [{{NAME}}_R{row}_C{col}.png]
  POSE:  [pose description from Step 4]
  NOTES: [notes from Step 4]
