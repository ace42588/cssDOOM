# {{SOR_DISPLAY_NAME}} — SGNL System of Record

Generated by [.cursor/skills/sgnl-sor](../.cursor/skills/sgnl-sor/SKILL.md).

## Files

| Path | Purpose |
|------|---------|
| `{{YAML_PATH}}` | SoR template uploaded to the SGNL Console |
| `{{ADAPTER_PATH}}` | Custom adapter source (omit for Event Push / SCIM) |
| `adapter.proto` | `sgnl.adapter.v1.Adapter` gRPC contract |

## Flow

`{{FLOW}}` — see [decision guide](../.cursor/skills/sgnl-sor/references/DECISION_GUIDE.md).

## Type

Adapter / SoR `type`: **`{{SOR_TYPE}}`**. Must match in three places:

1. `type` field in the YAML template above.
2. `RegisterAdapter(...)` (Go) / `SOR_TYPE` constant (Node, Python).
3. The Adapter row in the SGNL Console at **Admin → Adapters**.

## Run (polling adapter only)

```bash
export SGNL_ADAPTER_VALID_TOKENS='<openssl rand 64 | openssl enc -base64 -A>'
{{RUN_COMMAND}}
```

Send `token` gRPC metadata with one of `SGNL_ADAPTER_VALID_TOKENS` from
SGNL.

## Update

Re-run the skill (`/sgnl-sor`) and pick **update**. The skill will diff
the regenerated YAML / adapter against this directory before overwriting.
