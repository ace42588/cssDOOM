# {{DISPLAY_NAME}} — SGNL System of Record (custom polling adapter)
#
# Generated by .cursor/skills/sgnl-sor.
# `type` MUST match the type registered by the custom adapter
# (cmd/adapter/main.go for Go, server/sgnl/adapter for Node, etc.).
# `adapterConfig` is base64(JSON) — encode with scripts/encode_adapter_config.py.

displayName: "{{DISPLAY_NAME}}"
icon: "{{ICON_BASE64}}"
description: "{{DESCRIPTION}}"
address: "{{ADDRESS}}"
defaultSyncFrequency: {{DEFAULT_SYNC_FREQUENCY}}
defaultSyncMinInterval: {{DEFAULT_SYNC_MIN_INTERVAL}}
defaultApiCallFrequency: {{DEFAULT_API_CALL_FREQUENCY}}
defaultApiCallMinInterval: {{DEFAULT_API_CALL_MIN_INTERVAL}}
type: "{{SOR_TYPE}}"
adapterConfig: "{{ADAPTER_CONFIG_B64}}"
auth:
{{AUTH_BLOCK}}

entities:
{{ENTITIES_BLOCK}}

relationships:
{{RELATIONSHIPS_BLOCK}}
