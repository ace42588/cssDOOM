# {{DISPLAY_NAME}} — SGNL System of Record (SCIM 2.0)
#
# Generated by .cursor/skills/sgnl-sor.
# Uses SGNL's standard SCIM 2.0 adapter; no custom code required.
# `address` is the SCIM 2.0 base URL (SGNL appends /Users, /Groups, etc.).
# Set `pushType: SCIM2.0` and `deliveryMethod: entityPush` to receive
# entities pushed FROM cssDOOM/your service INTO SGNL.

displayName: "{{DISPLAY_NAME}}"
icon: "{{ICON_BASE64}}"
description: "{{DESCRIPTION}}"
address: "{{ADDRESS}}"
type: "{{SOR_TYPE}}"
{{DELIVERY_BLOCK}}
auth:
{{AUTH_BLOCK}}

entities:
{{ENTITIES_BLOCK}}

relationships:
{{RELATIONSHIPS_BLOCK}}
