# {{DISPLAY_NAME}} — SGNL System of Record (Event Push)
#
# Generated by .cursor/skills/sgnl-sor.
# Event Push templates do NOT need a custom adapter; events are POSTed to
# the SGNL endpoint returned at SoR creation time:
#   https://{clientName}.sgnlapis.cloud/events/custom/v1/{datasourceId}
#
# `pushEventsPath` selects the events array inside the inbound payload.
# Each entity declares an `externalId` value (constant) and a
# `pushExternalId` JSONPath that, when applied to an event, must equal
# `externalId` for the event to bind to that entity.

displayName: "{{DISPLAY_NAME}}"
description: "{{DESCRIPTION}}"
type: "{{SOR_TYPE}}"
deliveryMethod: eventPush
pushType: Custom
pushEventsPath: {{PUSH_EVENTS_PATH}}

entities:
{{ENTITIES_BLOCK}}
